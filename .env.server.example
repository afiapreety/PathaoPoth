# Optional server-side model connector. Never put the key in VITE_ variables.
# Configure a chat-completions-compatible endpoint to enable live note analysis.
AI_ENDPOINT=
AI_MODEL=
AI_API_KEY=
PORT=8787
